/**
 * Landing page screenshot paths. Replace these files in /public when assets are ready.
 *
 * Recommended captures (see landing page README or project docs):
 * - workspace: full project workspace — file tree, AI chat, preview panel
 * - agents: /agents grid with at least one agent card
 * - ai-task: AI chat mid-task with streaming answer or file changes visible
 */
export const LANDING_SCREENSHOTS = {
  workspace: "/hero-section-photo.png",
  agents: "/landing-agents.png",
  aiTask: "/landing-ai-task.png",
} as const;
